# Weather Alert Bot for Egyptian Cities

A Python script that monitors weather in major Egyptian cities and sends an email alert if the temperature exceeds 40°C.

## ✨ Features
- Checks temperature in 5 Egyptian cities: Cairo, Alexandria, Giza, Shubra El-Kheima, Port Said
- Sends email alert when temperature > 40°C
- Runs automatically every 6 hours
- Includes error handling for API and network issues

## 🛠️ Requirements
- Python 3.x
- OpenWeatherMap API key (free: [https://openweathermap.org/api](https://openweathermap.org/api))
- Gmail account with App Password enabled

## ▶️ How to Use
1. Install dependencies:
   ```bash
   pip install -r requirements.txt
